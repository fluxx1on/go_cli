# Client for thumbnail proxy-server

```
git clone https://github.com/fluxx1on/go_cli.git
go mod tidy

// Run it
make run-sync
make run-async

// Or
make build
./client (--async) --urls="url,url2,..." 
```